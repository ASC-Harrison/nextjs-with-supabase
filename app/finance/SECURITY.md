# Finance security
The finance tables use Row Level Security. Access requires an authenticated Supabase user who is a member of the matching finance household. The finance app does not use a service-role key in the browser.
